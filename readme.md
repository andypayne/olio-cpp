# Olio-CPP - C++ collections

- stk - A stack implementation, backed by `std::vector` (for now)

## Building a collection

Usually:
```shell
mkdir build
cd build
cmake ..
make
make test
# or
ctest -V
```
